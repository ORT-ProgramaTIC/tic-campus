{
  "name": "tic-campus-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp index.html dist/",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test test/*.test.mjs"
  }
}
